import type { HeaderProps } from "./header.props.autogenerated";
import { getHeaderData } from "./header.data";
import { HeaderView } from "./header.view";

export default async function HeaderModule(props: HeaderProps) { return <HeaderView {...props} data={await getHeaderData()} />; }
