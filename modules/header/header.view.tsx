import Link from "next/link";
import type { HeaderProps } from "./header.props.autogenerated";
import styles from "./header.module.scss";

interface HeaderData { siteName: string; links: Array<{ id: number; label: string; href: string }> }
export function HeaderView({ config, resources, data }: HeaderProps & { data: HeaderData }) {
  return <header className={`${styles.root} ${config.sticky ? styles.sticky : ""}`} style={{ backgroundColor: config.backgroundColor, color: config.foregroundColor }}><div className={`site-shell flex min-h-18 items-center gap-8 py-4 ${config.variant === "centered" ? "flex-col justify-center" : "justify-between"}`}><Link href="/" className="text-lg font-semibold tracking-tight">{data.siteName}</Link><nav aria-label={resources.menuLabel}><ul className="flex flex-wrap items-center justify-end gap-x-6 gap-y-2 text-sm">{data.links.map((link) => <li key={link.id}><Link className="hover:underline" href={link.href}>{link.label}</Link></li>)}<li><Link className="hover:underline" href="/blog">{resources.blogLabel}</Link></li>{config.showSearch ? <li><Link className="hover:underline" href="/search">{resources.searchLabel}</Link></li> : null}</ul></nav></div></header>;
}
